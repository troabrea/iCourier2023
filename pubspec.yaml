name: icourier
description: iCourier Mobile App.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 2023.7.8+2023694

environment:
  sdk: '>=3.2.0 <4.0.0'
#  sdk: ">=2.15.1 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter_bloc:
  flutter_svg:
  equatable:
  webview_flutter:
  url_launcher:
  flutter_native_splash:
  flutter_launcher_icons:
  google_maps_flutter:
  http:
  permission_handler:
  location:
  google_fonts: 6.1.0
  flutter_image_slideshow:
  flex_color_scheme:
  percent_indicator:
  connectivity_plus: ^5.0.1
  map_launcher: 3.1.0
  grouped_list:
  cached_network_image:
  empty_widget:
  easy_mask:
  get_it:
  user_profile_avatar:
  flutter_cache:
  flutter_form_builder:
  form_builder_validators:
  app_center_bundle_sdk:
    git:
      url: https://github.com/troabrea/app_center_bundle_sdk.git
      ref: master # branch name
  auto_size_text:
  form_builder_extra_fields:
  form_builder_image_picker:
  tutorial_coach_mark:
  package_info_plus: ^5.0.1
  firebase_core:
  firebase_messaging:
  flutter_local_notifications: ^16.1.0
  image_picker:
  quick_actions:
  barcode_widget:
  app_popup_menu: ^1.0.0
  im_stepper:
  another_stepper:
  file_picker:
  flutter_app_badger:
  font_awesome_flutter:
  event:
  azblob:
  app_bar_with_search_switch: ^1.3.6
  progress_stepper: ^1.0.0
  image_picker_android: ^0.8.5+2
  uuid:
  intl:
  easy_localization: ^3.0.2
  navbar_router:
  persistent_bottom_nav_bar_v2:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - translations/
#    - images/skyhigh/
    - images/clickpack/
#    - images/qm/
#    - images/domex/
#    - images/bmcargo/
#    - images/beexpress/
#    - images/almapaq/
#    - images/pintopaq/
#    - images/boxpaq/
#    - images/encargopaq/
#    - images/caribepack/
#    - images/cps/
#    - images/taino/
#    - images/fixocargo/
#    - images/picknsend/
#    - images/jetpack/
#    - images/tls/
#    - images/cainca/
#    - images/tupaq/
#    - images/swoop/
#    - images/priority/
    - images/noticias.svg
    - images/sucursales.svg
    - images/calculadora.svg
    - images/servicios.svg
    - images/preguntas.svg
    - images/arroba.svg
    - images/usps.png
    - images/ups.png
    - images/amazon.png
    - images/dhl.png
    - images/fedex.png
    - images/recibidomiami.svg
    - images/embarcado.svg
    - images/recibido.svg
    - images/disponible.svg
    - images/facebook.svg
    - images/pagweb.svg
    - images/correo.svg
    - images/facebook.svg
    - images/instagram.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Myriad
      fonts:
        - asset: fonts/MYRIADPRO-REGULAR.OTF
        - asset: fonts/MYRIADPRO-BOLD.OTF
          weight: 700
    - family: Continumm
      fonts:
        - asset: fonts/contl.ttf
        - asset: fonts/contm.ttf
          weight: 700
    - family: iCourier
      fonts:
        - asset: fonts/icourier.ttf
    - family: Bossa
      fonts:
        - asset: fonts/Bossa-Black.otf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages